{
  "name": "root",
  "private": true,
  "packageManager": "yarn@4.13.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "server:build": "yarn workspace server build",
    "server:start": "yarn workspace server start",
    "server:test": "yarn workspace server test",
    "client:build": "yarn workspace @eoneom/api-client build",
    "web:start": "yarn workspace swarm-web start",
    "web:build": "yarn workspace swarm-web build"
  }
}
